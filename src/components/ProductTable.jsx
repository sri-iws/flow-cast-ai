const tone = health => health === 'Healthy' ? 'good' : health === 'Watch' ? 'watch' : 'bad';
export default function ProductTable({ products, compact = false, onReorder, onReview }) { return <div className={compact ? 'risk-table' : 'full-table'}>{!compact && <div className="table-header"><span>Product</span><span>Category</span><span>On hand</span><span>30-day demand</span><span>Health</span><span>Recommendation</span></div>}{compact && <div className="table-header"><span>Product</span><span>Risk</span><span>Days left</span><span>Action</span></div>}{products.map(product => <div className={compact ? 'risk-row' : 'product-data-row'} key={product.sku}><div className="product-cell"><div className="product-thumb headphones"><img src={product.image} alt="" loading="lazy" /></div><div><strong>{product.name}</strong><small>{product.sku} · {product.category}</small></div></div>{compact ? <><span className={`risk-pill ${product.risk > 80 ? 'high' : 'medium'}`}>{product.risk}% {product.risk > 80 ? 'high' : 'watch'}</span><strong className={product.daysLeft < 14 ? 'days-critical' : ''}>{product.daysLeft} days</strong><button type="button" className="row-action" onClick={() => onReorder(product)} aria-label={`Recommend reorder for ${product.name}`}>{'Recommend'}</button></> : <><span>{product.category}</span><b>{product.stock.toLocaleString()}</b><span>{product.demand.toLocaleString()}</span><span className="health-label"><i className={`health-dot ${tone(product.health)}`} />{product.health}</span><span className="recommendation">{product.recommendation}</span>{onReorder && product.recommendation.toLowerCase().includes('reorder') && <button type="button" className="row-action" onClick={() => onReorder(product)} aria-label={`Reorder ${product.name}`}>Reorder</button>}</>}</div>)}</div>; }
